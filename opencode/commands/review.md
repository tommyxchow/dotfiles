---
description: Find real defects in code changes. Reports; fixes only when asked to fix. A pass run afterwards applies what it confirmed. quick is one read of the essentials; deep is the full run.
---

Load and follow the `review` skill. Use the user's arguments as its options: $ARGUMENTS
