id = "tc.worktree-bootstrap"
name = "Worktree bootstrap"
# herdr requires version; the plugin is a live link, so it is never bumped.
version = "0.3.0"
min_herdr_version = "0.9.0"
description = "Copy env files into a new worktree, install deps when the repo makes that cheap"
platforms = ["linux", "macos", "windows"]

[[events]]
on = "worktree.created"
command = ["node", "bootstrap.mjs"]
